pipeline{
    agent any
    stages{

        stage ('clean install stage'){
            steps{
                withMaven(maven : 'M3'){
                    sh 'mvn clean install -Dmaven.test.skip=true'
                }
             }
        }
    }
}
